/* Mo'Betta Green MarketPlace — coming soon page behavior */
(function () {
  'use strict';

  // Keep the copyright year honest without a build step.
  var year = document.getElementById('year');
  if (year) year.textContent = String(new Date().getFullYear());
})();
